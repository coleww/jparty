<div class="category cell">{{categoryTitle}}</div>
{{#each value in category.answers}}
  <div class="answer cell" {{action 'ask' category value}}>
    {{#if value.asked}}
    {{else}}
      ${{value.value}}
    {{/if}}
  </div>
{{/each}}